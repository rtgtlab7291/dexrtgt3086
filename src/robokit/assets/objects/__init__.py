"""Object meshes (grasping and human-object-interaction props).

Import a specific asset module (e.g. `from robokit.assets.objects import omomo`);
the package itself imports no submodule, so importing it triggers no download.
"""
