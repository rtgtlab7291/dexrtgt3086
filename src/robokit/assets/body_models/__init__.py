"""Parametric human body models (MANO, SMPL-X).

Import a specific asset module (e.g. `from robokit.assets.body_models import ...`);
the package itself imports no submodule, so importing it triggers no download.
"""
