"""Robot descriptions: URDFs, collision spheres and contact points.

Import a specific asset module (e.g. `from robokit.assets.robots import ...`);
the package itself imports no submodule, so importing it triggers no download.
"""
