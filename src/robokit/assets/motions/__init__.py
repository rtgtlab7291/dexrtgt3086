"""Motion-capture / retargeting clip datasets.

Import a specific asset module (e.g. `from robokit.assets.motions import ...`);
the package itself imports no submodule, so importing it triggers no download.
"""
